
import React from 'react';

const CreateCourseScreen = ({ onNext, onBack }: { onNext: (prompt: string, file: any) => void, onBack: () => void }) => {
  return <div>Create Course Screen Placeholder</div>;
};

export default CreateCourseScreen;
